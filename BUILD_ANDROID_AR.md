# IN THE VOID — Android APK

هذه النسخة هي مشروع Android مبني من آخر نسخة للموقع، والتطبيق يضم ملفات الموقع داخله بدل فتح رابط خارجي.

## البناء المجاني عبر GitHub Actions
1. ارفع محتويات هذا المجلد إلى Repository جديد على GitHub.
2. من تبويب **Actions** اختر **Build IN THE VOID APK**.
3. اضغط **Run workflow**.
4. بعد انتهاء البناء افتح الـworkflow ثم **Artifacts** ونزّل `IN_THE_VOID-APK`.

لن تحتاج على جهازك إلى Android Studio أو Android SDK أو Gradle؛ GitHub Actions يتولى بيئة البناء.

## البيانات
التطبيق يستخدم نفس Supabase الموجود في نسخة الموقع.

## البداية
التطبيق يبدأ بصفحة الفازة التفاعلية الموجودة في المشروع، ثم يفحص جلسة Supabase بصمت أثناء التفاعل. الحساب المسجل لا يرى شاشة Login؛ وعند غياب الجلسة فعليًا تظهر شاشة الدخول. عند تعذر استعادة الجلسة بعد المحاولات الاحتياطية يتم استخدام Session Guard.

## الملفات المحلية
في نسخة Android يتم حفظ الملفات التي تم تنزيلها محليًا داخل IndexedDB. عند الضغط مرة أخرى على نفس الملف يظهر **فتح** ويُفتح من النسخة المحلية دون تنزيل جديد.


## مهم للتحديثات
- بيانات المستخدم والملفات الموجودة في Supabase مشتركة مع الموقع.
- تغييرات الكود نفسها تحتاج Build APK جديد.
- الملفات التي تم تنزيلها من التطبيق تحفظ محليًا؛ حذف التطبيق أو بياناته يحذف النسخ المحلية.
